/*
Operador ternario

(condición) ? true:false

(condición) ?

    (primera sentencia,
     segunda sentencia)
*/

let num = 3;

(num%2==0) ? (console.log(`el número ${num} es par`)) : (console.log(`el número ${num} es impar`)); 