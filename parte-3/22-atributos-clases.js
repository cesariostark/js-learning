/*
ATRIBUTOS Y CLASES

Atributos

element.getAttribute('attribute') -> Devuelve el atributo
element.setAttribute('attribute', value) -> Asigna un valor al atributo

//console.log(name.getAttribute('type'));
//name.setAttribute('type', '');

Clases

element.classList.add('class', 'class', ....)
element.classList.remove('class', 'class', ....)
element.classList.toggle('class'[,force])
element.classList.contains('class')
element.classList.replace('oldClass', newClass)

atributos directos

id
value

*/

const title = document.getElementById('title');
const name = document.getElementById('name');

//title.classList.add('main-title', 'other-title');
//title.classList.remove('title');
//title.classList.contains('title');
//title.classList.replace('title', 'main-title');

/* console.log(title);
console.log(name);
 */
