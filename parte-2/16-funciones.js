/* FUNCIONES

- Son fragmentos de código que escribimos para ejecutar una tarea y no volver a escribir el mismo código más de una vez
- Nos ayudan a modularizar código. (ayuda a buscar errores)
- Las funciones deben realizar una sola tarea.

como escribir:

function nombreFunction(){
    codigo a ejecutar
}

const nombreFunction = () => {
    codigo a ejecutar
}

- Pueden recibir parámetros

    const nameFunction = (parametro1, parametro2) => { //function}
*/

/* const saludar = () =>{
    console.log('Hola');
}
const saludarUsuario = (user) =>{
    console.log(`Hola ${user}`);
}

saludar();
saludarUsuario('cesario'); */

/* const suma = (num1, num2) => {
    return num1+num2
}

console.log(suma(2,6)); */